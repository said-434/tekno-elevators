# Copy to .env and fill values
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
TO_EMAIL=tekno.elevators55@gmail.com
FROM_EMAIL=you@yourdomain.com
# Optional: Use SendGrid API key instead of SMTP. If set, SendGrid will be used.
SENDGRID_API_KEY=
PORT=3000
